import React from "react";
import { withRouter } from "react-router";

const Home = () => {
  return (
    <div
      id="home">
      <div id="homeContent">
        <div className="page-title">Home Page</div>
        <h3> Welcome! </h3>
      </div>
    </div>
  );
};

export default (Home);